def change_theme_color(color_list):
    for color in color_list:
        print(f"Updating theme to {color}")
        # Here you would add the code to actually change the theme in the app
        # For example: app.set_theme(color)

# Example usage
available_colors = ["blue", "green", "red", "purple"]
change_theme_color(available_colors)